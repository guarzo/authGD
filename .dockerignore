node_modules
.next
.git
.claude
docs
# e2e/ is deliberately NOT listed here. tsconfig.json type-checks `**/*.ts`,
# and playwright.config.ts + scripts/e2e-clean.ts import from e2e/ — ignoring
# it makes `next build` fail inside the image with TS2307 while CI, which
# builds the full checkout, stays green. It reaches the build stage only; the
# runner stage copies neither it nor tests/.
tests
tmp
docker-compose.dev.yml
*.md
.env*
